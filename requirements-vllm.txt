# =============================================================================
# vLLM 서빙 전용 — 반드시 별도 venv (python3.10 -m venv .venv-vllm)
# vLLM 이 자체 torch를 끌어오므로 requirements-train.txt 와 공존 불가.
# =============================================================================
#
# pip install -r requirements-vllm.txt
# python scripts/serve_vllm.py
# =============================================================================

vllm==0.19.0
